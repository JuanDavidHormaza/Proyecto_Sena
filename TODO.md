# TODO - Proyecto_Sena

- [x] Fix inmediato del crash: eliminar `mockTestResults` no definida en `AdminDashboard.tsx`.
- [ ] Arreglar creación de usuarios (guardar correctamente) en endpoint `/api/users/`.
- [ ] Arreglar actualización de usuario (program) en `backend/users/Views/api_views.py` (hay código muerto después de `return`).
- [ ] Validar delete user funciona con `UserViewSet.destroy`.
- [ ] Probar flujo: agregar usuario → recargar lista → modificar → eliminar.

